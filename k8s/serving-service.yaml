apiVersion: v1
kind: Service
metadata:
  name: ml-serving-service
  namespace: ml-training
  labels:
    app: ml-serving
spec:
  type: ClusterIP
  selector:
    app: ml-serving
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
